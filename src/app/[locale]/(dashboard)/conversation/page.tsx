import React from "react";

const conversation = () => {
    <div className="flex flex-col gap-6 mx-3 flex-1 items-center justify-center  h-full max-h-full overflow-y-auto">
        Select a Chat To Start a Conversation
    </div>;
};

export default conversation;
